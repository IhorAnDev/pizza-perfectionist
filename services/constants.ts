export enum ApiRoutes {
  SEARCH_PRODUCTS = "/products/search",
  GET_INGREDIENTS = "/ingredients",
  GET_CATEGORIES = "/categories",
  GET_PRODUCTS = "/products",
  GET_PRODUCT = "/products",
  GET_PRODUCT_BY_CATEGORY = "/products",
}
